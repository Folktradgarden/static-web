import styled from "styled-components"

export const Circle = styled.path`
  fill: ${({ theme }) => theme.colors.instagram};
`
export const Path = styled.path`
  fill: ${({ theme }) => theme.colors.surface};
`
